1. 前端参考 ui_infer 里面的风格来实现
2. 数据库使用postgresql和redis，已部署，请参考D:\work\创新产品部\code\ai_inspiration_kit\backend\.env 文件中的配置，使用相同的数据库的地址，但是需要注意，不要使用跟之前这个项目相同的数据库，比如postgresql需要注意修改用户名/数据库名/密码等，然后redis可以使用index=2的数据库（0,1都被占用了）.
3. 悬浮球功能的实现，请参考 D:\work\创新产品部\code\ai_inspiration_kit\frontend 里面关于floatball的实现，可以完全照搬其核心逻辑与实现
4. 在实现核心逻辑后，一定需要再写单元测试和集成测试的代码，测试产品前后端核心功能的正确性，要保证前后端核心功能和API的100%覆盖，尽可能覆盖多的不同用户使用场景和反例、异常等场景。